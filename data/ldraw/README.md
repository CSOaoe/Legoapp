# LDraw source data
Extract an official LDraw library here so `parts/` contains `.dat` files, then run `brickforge import-ldraw data/ldraw`. Only direct, unambiguous part-number matches are attached in V0.1.
