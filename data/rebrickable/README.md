# Rebrickable source data
Download and extract the current CSV files from https://rebrickable.com/downloads/ here. Required: `parts.csv`, `part_categories.csv`, and `colors.csv`. Optional: `elements.csv` and `inventory_parts.csv`.

Run `brickforge import-rebrickable data/rebrickable`. Source CSVs are ignored by Git.
